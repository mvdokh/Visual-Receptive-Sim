"""
Logical GUI panels that live in the main Dear PyGui window.

The current `app.py` inlines a minimal set of controls; these panel
modules are the right place to move more complex layouts as the UI
grows (controls, firing panel, cell inspector, RF panel, export tools).
"""

