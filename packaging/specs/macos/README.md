PyInstaller writes a generated `.spec` here when you run `python packaging/build.py`.

The canonical app icon is **`rgc_simulator_icon.svg`** at the repo root; `build.py` rasterizes it to `packaging/icons/app.icns` before building the `.app` bundle.
